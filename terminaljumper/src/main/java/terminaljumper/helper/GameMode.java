package terminaljumper.helper;

/**
 *<h1>GameMode</h1>
 * This enum holds the two constants Classic and Expert.
 * These two modes differs from each other, where Classic is set to regular speed
 * and Expert is set to a higher speed.
 */
public enum GameMode {
    CLASSIC,
    EXPERT
}
